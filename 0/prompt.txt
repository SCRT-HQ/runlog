I would like to start planning out an integration for Elgato Stream Decks to allow full run control (progress through screens, trigger rolls when required, etc) as well as provide Stream Deck widgets that can show current metrics from the active run in a tab.

---

B looks good

---

this makes sense. With the OAuth setup, how does a run get chosen to attach to?

---

this sounds good, we need to make sure we also expose a way to change the run that SD is attached to, e.g. Stream Deck will almost always be launched before a run is started. Another approach would be sending a message over the socket when a new run is launched so the switcher in SD always knows the current list of available runs to choose from. We may also want to show a Toast notification when a run is connected from Stream Deck since the SD itself is not the best UI, especially on older models without the LCD screen or dials

---

we should also fetch the runs on launch / connect of the stream deck if those widgets are actively configured

---

Let's implement

---

are we creating a full plugin or do I need to use something like a community built WS plugin to bridge the gap?

---

why are we writing docs and committing them instead of using Claude artifacts?

---

<task-notification>
<task-type>artifact-watch-lifecycle</task-type>
<summary>Not watching Artifact: "The Deck Drives" (watch limit reached)</summary>
<event>The live subscription for https://claude.ai/code/artifact/442de4ff-fd8f-42fd-b511-ba7606b82e1b did not arm — this session already holds its maximum of 5 artifact watches and none could make room (each is a watch you requested, one auto-replying to comments, or the artifact you most recently published, or watch slots are still connecting); unwatch one first. This session is NOT watching it and will not hear when it is republished; pass action "watch" with its url if you still need that, and do not claim to be watching it meanwhile.</event>
</task-notification>

---

looks good -- for the open question, yes, this should be part of Plus

---

it should remain local for the spec, .claude/* is gitignored intentionally

noted on the 2 surfaced items, makes sense

---

We should also switch to Fable for this (just switched models)

---

1.

---

<task-notification>
<task-id>aac464d4ebf7b20c5</task-id>
<tool-use-id>toolu_0118KzkUc3kR2fb6kRwvvzes</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\aac464d4ebf7b20c5.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 1: deck connections" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>- **Status:** DONE
- Commit: 5add212 feat(live): a connection can say it is a deck
- Test summary: full hosted/infra suite 274/274 passing; typecheck clean; TDD RED confirmed (`live.decksOf is not a function`) before GREEN
- Concerns: none — `handlers.test.ts` left untouched since no existing pattern mocks `dynamoLive`'s SDK calls; `decksOf`/deck-aware `connect`/`disconnect` paths in `dynamoLive` are covered by typecheck only, same as the rest of that function today
- Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-1-report.md</result>
<usage><subagent_tokens>104882</subagent_tokens><tool_uses>25</tool_uses><duration_ms>194844</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ae538b1c0aebea2dd</task-id>
<tool-use-id>toolu_01ETPrAtVZnoGewaWXtyFuGu</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\ae538b1c0aebea2dd.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 1 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Named risk checked: `LiveStore` has exactly two implementations — `dynamoLive` (`hosted/infra/lib/handlers/live.ts`) and `memoryLive()` (`hosted/infra/test/ws.test.ts`); `ws.ts` only consumes the type, doesn't implement it. Both implementations in the diff gained `decksOf`, so no implementation is left non-conforming.

### Spec Compliance
- ✅ Spec compliant. All amended-brief requirements are present: `Attached.deck?: boolean` with the specified comment (live.ts:33), single spelling point in `marks()`/`read()` (live.ts:60, live.ts:70), `upk` helper (live.ts:78), third-row write in `connect()` guarded by `attached.deck` with matching pk/sk shape and shared `expiresAt` (live.ts:99-107), `decksOf` query with TTL filtering (live.ts:130-142), `disconnect()` cleanup of the `USER#`/`DECK#` row keyed off the `CONN` row's `sub` (live.ts:152-153), `LiveStore.decksOf` interface addition (live.ts:47-48), `memoryLive().decksOf` (ws.test.ts:179-183), and the `decksOf` contract test against `memoryLive()` in place of the brief's `dynamoLive`/`makeLive()` version per the ruling (ws.test.ts:205-217). Only `hosted/infra/lib/handlers/live.ts` and `hosted/infra/test/ws.test.ts` are touched; `handlers.test.ts` was correctly left alone per the ruling.
- ⚠️ Cannot verify from diff: the diff's commit list shows only the one-line subject (`feat(live): a connection can say it is a deck`), not the full commit message body, so the "no Claude trailer/session link/footer" constraint and the report's claim of an unrelated repo `Entire-Checkpoint:` trailer can't be confirmed from this diff package alone.

### Strengths
- `marks()`/`read()` stay the single place a mark is spelled — `deck` follows the exact ternary idiom already used for `control`/`seat`/`app`/`run` (live.ts:56-61, 66-71).
- Test double coverage is real behavior, not mocked internals: the `decksOf` test (ws.test.ts:205-217) exercises a plain (non-deck) connection alongside a deck connection on the same account (`c1` deck, `c2` plain, both `user_a`), a second account's deck (`c3`, `user_b`), and disconnect-removes-the-deck — all three edge cases named in the review brief are covered in one compact test.
- `disconnect()`'s deck-row cleanup correctly short-circuits before the `WATCH#` branch by keying off `sk === "CONN"`, so no double-delete or missed branch (live.ts:152-156).
- Third-row TTL and shape match the two existing row-pair patterns (`CONN`/`WATCH#`) exactly — `expiresAt` shared from the same `connect()` call, `kind: "deck"` mirrors `kind: "conn"`, filtering in `decksOf` mirrors `watchers()`'s `expiresAt` filter (live.ts:88-107, 138-141 vs the pre-existing `watchers()` block).
- No scope creep: nothing beyond what the interface/rows require was added.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- ws.test.ts:207 — `const live = memoryLive();` omits the `const live: LiveStore = ...` annotation the brief's snippet used. Harmless here since `memoryLive()`'s own declared return type (`LiveStore &amp; {...}`) already enforces the contract, but worth noting since the brief's Step 1 snippet explicitly typed it that way to make the "is this still a `LiveStore`" check visible at the call site.
- live.ts: the new `DECK#` row's `marks(attached)` will also copy any other truthy `Attached` fields (`seat`, `app`, `run`, `control`) onto the deck row if a caller ever passes them alongside `deck: true`. Not incorrect per the brief (which specifies the same `...marks(attached)` spread), but it means `decksOf()`'s results carry whatever else was attached at connect time rather than only deck-relevant fields — worth a one-line comment if this becomes a real combination in practice.

### Assessment
**Task quality:** Approved
**Reasoning:** The diff implements every amended-brief requirement precisely, reuses existing patterns (`marks`/`read`, two-row write/query/cleanup shape, TTL filtering) without deviation, and the new test exercises all three required edge cases against a real (non-mocked) double; only an unverifiable commit-trailer detail and two cosmetic minor notes remain.</result>
<usage><subagent_tokens>75376</subagent_tokens><tool_uses>5</tool_uses><duration_ms>84795</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aee5f6ab87f4d2e38</task-id>
<tool-use-id>toolu_01Qzm1nXhuk6YvxmQ8kLezcC</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\aee5f6ab87f4d2e38.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 2: deck attaches" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commits: b9f395d feat(live): a deck attaches on the socket
Tests: ws suite 34/34, full hosted/infra suite 276/276, typecheck clean
Concerns: none — only deviation from brief is using the file's real `deps()`/`ev()` helpers and fixture values (`user_1`, `t=livetok`/`run=open`) instead of the brief's illustrative `depsWith`/`user_a`/`tok`/`s1`, which the task instructions explicitly authorized
Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-2-report.md</result>
<usage><subagent_tokens>82835</subagent_tokens><tool_uses>19</tool_uses><duration_ms>169870</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a1b30cdb83e2ac8cc</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\a1b30cdb83e2ac8cc.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 2 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed the fixture: `getSession("open")` returns `publicTokenHash: hashToken("livetok")` (test/ws.test.ts:116), so `t=livetok&amp;run=open` genuinely reaches the live-link branch (statusCode 200) rather than falling through to a 401. This is the same pattern used elsewhere in the file (line 380 for the `as=control` case).

### Spec Compliance
- ✅ `deckOf(event)` added at `hosted/infra/lib/handlers/ws.ts:74-76`, comment verbatim from the brief (diff lines 27-38).
- ✅ Signed-in `$connect` branch marks `{ deck: true }`: `ws.ts:140-141`, `const deck = deckOf(event); await deps.live.connect(connectionId, caller.sub, now(), deck);` — read only after `deps.verify` succeeds (`ws.ts:134-139` try/catch precedes it), satisfying the named ordering risk.
- ✅ Live-link branch (`t`+`run`, `ws.ts:87-96`) and watch-key branch (`k`, `ws.ts:110-132`) are untouched by the diff (only `attachedOf` is called there) and use `attachedOf`, which only recognizes `as=control` (`ws.ts:59-63`, unchanged) — `as=deck` on either is inert by construction, matching the global constraint.
- ✅ Test 1 (`ws.test.ts:187-190`) exercises the real signed-in path (`token: "good"` → `sub: "user_1"` per the file's existing `deps()`/`verify` fixture) and asserts `{ sub: "user_1", deck: true }` against a live `memoryLive` store where `marks.set` only fires when `attached` is truthy (`ws.test.ts:23-25`) — a real positive check, not vacuous.
- ✅ Test 2 (`ws.test.ts:192-196`) uses `t: "livetok", run: "open", as: "deck"`; `getSession("open")` fixture has `publicTokenHash: hashToken("livetok")` (`ws.test.ts:116`), so the request genuinely reaches the live-link branch (200), and `connection("c9")` returns a real object `{ sub: "public:open" }` (not null/undefined, since `conns.set` always runs) — so `.not.toMatchObject({ deck: true })` is a meaningful negative check, not one that passes only because the record is missing.
- ✅ Only `hosted/infra/lib/handlers/ws.ts` and `hosted/infra/test/ws.test.ts` touched (diff stat: 2 files, 31 insertions, 1 deletion).
- ✅ Brief's illustrative helper names (`depsWith`, `user_a`, `tok`, `s1`) correctly swapped for the file's real `deps()`/`ev()` and fixtures (`user_1`, `good`, `livetok`/`open`), per the task's explicit authorization; assertion shapes preserved.
- ⚠️ Cannot verify from diff: commit body content (no Claude trailer/Co-Authored-By/session link) — the diff file's commit list shows only the subject line `b9f395d feat(live): a deck attaches on the socket`, which matches the required exact subject, but the full commit message body (trailers) isn't part of the diff text and per instructions git commands were not re-run to check it.
- ⚠️ Cannot verify from diff: `npm run typecheck` and full-suite pass claims (276/276) — test re-run was not performed per review instructions; report's RED/GREEN transcripts are internally consistent with the code shown but unverified independently.

### Strengths
- `deckOf` mirrors `attachedOf`'s established shape exactly: pure function on `event`, strict equality on `as`, returns a typed marker object or `undefined` (`ws.ts:59-63` vs `74-76`).
- The one-line integration into the signed-in branch is minimal and doesn't disturb the two untouched branches, keeping the blast radius exactly at the brief's boundary.
- Test 2 isn't a throwaway regression guard dressed up as new coverage — it targets a real fixture path (`open`/`livetok`) already used elsewhere in the file, so it actually exercises the live-link branch rather than short-circuiting on a 401.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- The report notes (accurately) that test 2 already passed before `deckOf` existed, since `attachedOf` never reads `"deck"`. That's expected given the branch separation, but it means test 2 alone would not have caught a regression where someone wired `deckOf` into the wrong branch — only test 1 combined with the branch-isolation-by-construction argument closes that gap. Not a defect since the brief specified exactly this test, just worth naming.

### Assessment
**Task quality:** Approved
**Reasoning:** The implementation matches the brief precisely (verbatim comment, correct branch, correct signature), respects the deps.verify-before-deckOf ordering and branch-isolation constraints confirmed directly in the diff, and both new tests exercise real fixture-backed behavior rather than trivially-passing assertions.</result>
<usage><subagent_tokens>78740</subagent_tokens><tool_uses>8</tool_uses><duration_ms>99970</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aab28aa6fa7722e80</task-id>
<tool-use-id>toolu_015n3fYzfCu7SHYQAMYCeSpP</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\aab28aa6fa7722e80.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: held-run list" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commits: 64f6bd8 feat(live): a deck is told which runs are held
Tests: full infra suite 277/277 passing; typecheck clean
Concerns: none
Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-3-report.md</result>
<usage><subagent_tokens>121273</subagent_tokens><tool_uses>29</tool_uses><duration_ms>265925</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aecaee5c73e420268</task-id>
<tool-use-id>toolu_01BdfGwzUKXtu9ikkBwdu8N3</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\aecaee5c73e420268.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 3 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>### Spec Compliance
- ✅ `writes()` is module-level, defined above `route` (review-diff lines ~78–87, `hosted/infra/lib/handlers/ws.ts`) — excludes `control`, `deck`, and `sub` prefixed `public:`/`stream:`, matching the "held" definition exactly.
- ✅ `heldRuns`/`tellDecks` are defined inside `route`, closing over `deps`/`now`, before the `$connect` block (diff lines ~95–138) — matches the controller's placement ruling (annotate() staying between `const now` and the new helpers is pre-existing code, not a relocation issue).
- ✅ `heldRuns` filter/sort/cap exactly as specified: `role === "owner" &amp;&amp; !deletedAt &amp;&amp; !endedAt` → sort newest `updatedAt` first → `.slice(0, 20)` → then (and only then) the `watchers()` check (diff lines 106–109). Confirms named risk 1: the per-candidate `watchers()` await is bounded by the 20-cap, not the full manifest.
- ✅ Entry shape `{ id, name?, packTitle?, held: true }` with `name`/`packTitle` included only when truthy (diff lines 112–117).
- ✅ `$connect` signed-in branch wired exactly per the controller's literal ruling: `const deck = deckOf(event); await deps.live.connect(...); if (deck) await tellDecks(caller.sub, connectionId); return { statusCode: 200 };` (diff, second hunk, `hosted/infra/lib/handlers/ws.ts`).
- ✅ `tellDecks` posts to `only` when given, else `decksOf(sub)`, and disconnects on a `"gone"` reply (diff lines 128, 133).
- ✅ Test uses `ws.test.ts`'s real `deps()`/`ev()` helpers, signed-in `token: "good"` → `user_1` fixture, manifest extended minimally with `s1` (name "Thursday", packTitle "The Long Kiln") and `s2` (neither) — `hosted/infra/test/ws.test.ts` lines 138–154 (fixture), 177–195 (test). The `toEqual` assertion on the full `runs` array proves both inclusion of the held run and exclusion of the unheld one in one check.
- ✅ Commit subject exactly `feat(live): a deck is told which runs are held`; only the two named files touched (diff stat: 2 files, 84 insertions, 1 deletion).
- ❌ **Named risk 2, second half** — "a failed post cannot fail the `$connect`": the posted line is correctly targeted at the deck's own `connectionId` (`hosted/infra/test/ws.test.ts:193` confirms via `posted.find(([id]) =&gt; id === "deck1")`), but `tellDecks`'s `try/catch` wraps only `poster.post`/`disconnect` (diff lines 131–137). The line building it — `const line = JSON.stringify({ t: "runs", runs: await heldRuns(sub) });` (diff line 130) — sits *outside* that try/catch. If `deps.store.manifest` or `deps.live.watchers` inside `heldRuns` rejects, the rejection propagates out of `tellDecks`, out of the `if (deck) await tellDecks(...)` call in `$connect` (unguarded there too), and out of `route()` itself — so `$connect` would not return 200, and the error would not be "logged... and never thrown" as the global constraint on `tellDecks` requires. This exact unguarded structure was given verbatim in the brief's Step 3 code, so it is a plan-mandated defect, not something the implementer introduced independently.
- ⚠️ Cannot verify from diff: whether the `console.error("live: could not say which runs are held", ...)` prefix matches the convention used elsewhere in `ws.ts` (not shown in the diff; not one of the two sanctioned outside-diff risks, so not checked further).
- ⚠️ Cannot verify from diff: whether `tellDecks`'s post/disconnect loop meaningfully reuses/mirrors `tellWhoIsAttached`'s loop rather than duplicating it verbatim — `tellWhoIsAttached` itself is unchanged and not shown in the diff.

### Strengths
- `heldRuns` and `writes()` reproduce the brief's filter/sort/cap/shape logic precisely, with the twenty-cap correctly ordered before the `watchers()` fan-out (bounded concurrency, confirmed named risk 1).
- The new test exercises real production wiring (`deps()`, `ev()`, real `token: "good"` fixture) rather than a hand-rolled stub, and its exact-equality assertion proves both that the held run appears with the right fields and that the unheld run is absent — not just a shallow "was something posted" check.
- Fixture extension is minimal and non-disruptive: added `s1`/`s2` without touching `getSession` or the existing "open"/"shared" entries other tests depend on (`hosted/infra/test/ws.test.ts:138–154`).
- `$connect` wiring matches the controller's literal ruling character-for-character.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
- **Plan-mandated:** `tellDecks`'s `heldRuns(sub)` call is not covered by the function's `try/catch` (`hosted/infra/lib/handlers/ws.ts`, diff line 130, inside the `tellDecks` body at diff lines 125–138). A rejection from `deps.store.manifest` or `deps.live.watchers` (both awaited inside `heldRuns`) escapes `tellDecks` uncaught, propagates through the unguarded `if (deck) await tellDecks(caller.sub, connectionId);` in `$connect` (second diff hunk), and would make `route()` reject instead of returning `{ statusCode: 200 }`. This violates both the reviewer-flagged risk ("a failed post cannot fail the `$connect`") and the task's own global constraint that `tellDecks` errors are "logged... and never thrown." The unguarded placement was given verbatim in the brief's Step 3 code, so this is inherited from the plan rather than introduced by the implementer — still needs a fix (move the `heldRuns` call inside the try, or wrap it separately) before this can be trusted under real backend failures.

#### Minor (Nice to Have)
- No dedicated test for "a run held only by a `deck` watcher" or "a run held only by a `public:` link watcher" — `writes()`'s exclusions for these cases are correct by inspection but untested directly. The brief's Step 1 only required the one posted-line test, so this isn't a missed requirement, just uncovered by the new suite.
- `console.error("live: could not say which runs are held", error)` prefix consistency with the rest of `ws.ts` isn't visible in the diff (flagged above as ⚠️, not independently checked per the review's outside-diff restriction).

### Assessment
**Task quality:** Needs fixes
**Reasoning:** The implementation matches the brief and controller rulings closely — helper placement, filtering/sorting/cap logic, wire shape, and the `$connect` wiring are all correct, and the new test genuinely verifies held-inclusion/unheld-exclusion. But the reviewer-flagged risk about `tellDecks` not being allowed to fail `$connect` turns up a real gap: `heldRuns()`'s call is unguarded by the try/catch, so a backend failure during connect would break the 200-response guarantee and violate the "never thrown" constraint — inherited from the brief's own sample code, but still a defect that needs fixing before this can be trusted in production.</result>
<usage><subagent_tokens>82696</subagent_tokens><tool_uses>3</tool_uses><duration_ms>168657</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aab28aa6fa7722e80</task-id>
<tool-use-id>toolu_01UAXQP3Yq1tn1jEooXMGNK5</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\aab28aa6fa7722e80.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: held-run list" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE (fix round 1 addressed)
Commits: 64f6bd8 feat(live): a deck is told which runs are held; 49869b2 fix(live): a deck attaches even when its run list cannot be built
Tests: `npm test -w hosted/infra -- ws` 36/36 passing; typecheck clean
Concerns: none
Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-3-report.md</result>
<usage><subagent_tokens>131371</subagent_tokens><tool_uses>12</tool_uses><duration_ms>97271</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a03d33b8d05c980bf</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\a03d33b8d05c980bf.output</output-file>
<status>completed</status>
<summary>Agent "Re-review Task 3 fix round 1" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Good — `WsDeps` is already imported, and the pattern `const d: WsDeps = { ...base, store: {...}, poster: {...} }` matches an existing usage at line 394, so this isn't a new pattern. The fix diff is self-contained and type-checks per the report.

### Finding Verdicts

**heldRuns unguarded in tellDecks** — ADDRESSED. `hosted/infra/lib/handlers/ws.ts` (diff lines ~24-48 of the review diff): `tellDecks`'s entire body — the `decksOf` read, `const line = JSON.stringify({ t: "runs", runs: await heldRuns(sub) })`, and the post loop — is now inside one `try { … } catch (error) { console.error("live: could not say which runs are held", error); }`. No `await` in the function's happy path sits outside the guard, so a `manifest`/`watchers` rejection inside `heldRuns` can no longer escape `tellDecks`. Since `tellDecks` never rejects, `if (deck) await tellDecks(...)` in `$connect` no longer propagates a rejection, and `route()` still reaches `return { statusCode: 200 }`. `hosted/infra/test/ws.test.ts:227-251` adds `"still attaches a deck when its run list cannot be built"`, which overrides `store.manifest` to throw, drives a real `$connect` as a deck, and asserts `res.statusCode === 200` and `posted` is empty — proving both that the connect still succeeds and that no partial/garbage post went out. This matches the controller's ruling and the specific defect (manifest rejection escaping) is gone.

### New Breakage in the Fix Diff
None at Critical/Important severity. One behavioral note below (non-blocking, and it's the literal shape the controller's ruling mandated).

### Out-of-Scope Observations
- `hosted/infra/lib/handlers/ws.ts` (new `tellDecks` shape): wrapping the whole function body in one `try` instead of keeping the per-iteration `try/catch` around `poster.post`/`disconnect` inside the loop means that if posting to one deck's connection throws mid-loop, the remaining decks in that `decksOf(sub)` batch are skipped for this push (they'll only pick up the held-run list on the next change, not this one). This is a minor regression in fan-out coverage versus the pre-fix per-post isolation, but it is exactly the shape the controller's ruling specified ("the whole body of `tellDecks` goes inside a try/catch"), so it's not something the implementer introduced beyond the mandate — flagging only for awareness, not blocking.
- Test coverage under the fix targets `deps.store.manifest` rejecting; it does not add a second test for `deps.live.watchers` rejecting. The finding's "(or watchers)" phrasing reads as either being sufficient for one test, and `heldRuns`'s `await Promise.all(mine.map(... deps.live.watchers ...))` sits inside the same try as `manifest`, so the guard covers both paths identically — no functional gap, just noting the untested branch is structurally identical to the tested one.

### Verdict
**Fix round:** All findings addressed, no new Critical/Important breakage.</result>
<usage><subagent_tokens>70180</subagent_tokens><tool_uses>4</tool_uses><duration_ms>51460</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>adc7f7f3c9f81fa86</task-id>
<tool-use-id>toolu_01P6ff2fFtJcPzEdwjGhUY5w</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\adc7f7f3c9f81fa86.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 4: runs pushed on change" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: 8e094a0 feat(live): the held-run list is pushed when it changes
Tests: hosted/infra suite 280/280 passing (ws.test.ts 38/38 incl. 2 new); RED confirmed pre-implementation via stash; typecheck clean
Concerns: none
Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-4-report.md</result>
<usage><subagent_tokens>123317</subagent_tokens><tool_uses>34</tool_uses><duration_ms>360655</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>af7649490631e48dc</task-id>
<tool-use-id>toolu_0147pzEjgfte9ukvkfEBDkph</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\af7649490631e48dc.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 4 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>### Spec Compliance
- ✅ `watch` branch: `deps.live.watch(...)` unconditional, then `if (conn.deck)` re-writes CONN row via `deps.live.connect(connectionId, conn.sub, now(), { deck: true, run: m["id"] })` and calls `tellWhoIsAttached(m["id"])`, then unconditionally `await tellDecks(conn.sub)` — `hosted/infra/lib/handlers/ws.ts:557-566`. Order matches the ruling exactly (tellDecks after tellWhoIsAttached for a deck).
- ✅ `$disconnect`: existing control line untouched (`ws.ts:237`), ruling's deck line added beside it (`ws.ts:238-240`), brief's `tellDecks` line added (`ws.ts:241-243`). All three conditions verified against `writes()` (`ws.ts:85-87`): public:/stream: excluded (prefix check), attached tool excluded (`!w.control`), deck's own excluded (`!w.deck` in `writes()` and redundantly `!leaving.deck` in the guard), writing page included.
- ✅ New test count and shape: `describe("the held-run list is pushed when it changes", ...)` (`ws.test.ts:341-390`) contains (1) the brief's Step-1 test verbatim in substance (`ws.test.ts:342-362`) and (2) exactly one ruling-mandated test (`ws.test.ts:369-389`) asserting `live.connection("deck1")` matches `{ deck: true, run: "s1" }` and a `{ t: "gesture", kind: "tools" }` line posted to `"page"`, with no `decks` field asserted.
- ✅ Fixture addition is minimal and reuses the pre-existing `s1`/"The Long Kiln" convention already established in `manifest()` at Task 3 (`ws.test.ts:94-95`, `222`), not a new invented title — traced and confirmed no boundary violation and no conflict with any other test that references `getSession("s1")`.
- ✅ Comments: both brief comments used verbatim (`ws.ts:564-565`, `241-242`); both ruling additions carry exactly one short why-comment each (`ws.ts:238-239`, `559-560`), matching "one short why-comment" constraint.
- ✅ Commit subject exact match: `feat(live): the held-run list is pushed when it changes` (diff header, single commit `8e094a0`).
- ✅ Only the two allowed files touched (diff stat: `ws.ts` +15, `ws.test.ts` +52, no other files).
- ⚠️ Cannot verify from diff: the reported TDD RED/GREEN transcript and `npx prettier --write` run (not re-executed per instructions; code trace below stands in for it).

### Strengths
- The two named risks both check out under direct trace, not guesswork: `memoryLive().connect` does `marks.set(id, attached)` (`ws.test.ts:23-26`), a full replace, so `leaving.run` is correctly populated for a deck's CONN row after the ruling's re-write — confirmed independently by test assertion `ws.test.ts:386`.
- New tests assert real state (`live.connection("deck1")`) and real posted payload content (`JSON.parse`), not call-count spies — matches the review's bar for "do the tests verify real behavior."
- Four disconnect kinds (public:/stream:, attached tool, deck's own, writing page) all traced against `writes()` and the new guard and behave as specced — `ws.ts:85-87`, `241-243`.
- Non-owner-member watch and deck-not-a-member watch both correctly gated by the pre-existing, unchanged membership check (`ws.ts:552-556`) before any of the new deck logic runs — the new code adds no new authorization surface.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `ws.ts:218` (`tellWhoIsAttached`'s `await deps.live.watchers(run)`) sits outside any try, same as the pre-existing control-tool call sites at `ws.ts:237` and `ws.ts:289`. The diff extends this same unguarded shape to two new call sites: `ws.ts:240` (deck disconnect) and `ws.ts:562` (deck watch). Net-new consequence unique to the watch branch: if `live.watchers` rejects inside the `if (conn.deck)` block, the throw propagates past the unconditional `await tellDecks(conn.sub)` at `ws.ts:566`, so a deck's watch could 500 without the "deck or not, tellDecks" push firing. This matches the file's existing risk tolerance (top-level `try/catch` in `handler()`, `ws.ts:589-594`, downgrades any such throw to a logged 500 rather than crashing), so it's not a regression in kind, just an additional trigger of an accepted pattern.
- `tellWhoIsAttached(m["id"])` at `ws.ts:562` fans out to all non-control watchers of the run, which — traced through `ws.ts:557`'s unconditional `live.watch(connectionId, m["id"], conn.sub, now())` — now includes the deck itself (it becomes a watcher of the run it just watched). The deck therefore also receives its own `{t:"gesture", kind:"tools"}` broadcast. Neither test asserts this either way (`ws.test.ts:387` filters only for `"page"`). The report flags this class of gap as deliberately deferred ("tellWhoIsAttached counts nothing deck-shaped yet, Task 6's job"), which is consistent with the ruling's brief not asking for any exclusion — noted for completeness, not a defect against this task's scope.

### Assessment
**Task quality:** Approved
**Reasoning:** Every brief and ruling requirement is present at the cited lines, ordering and comment placement match verbatim where specified, the two named risks were traced and confirmed safe (memory double's `connect` overwrites, not merges; the unguarded-rejection shape is pre-existing and now merely extended, not newly introduced), and the new tests verify real posted-line content and real CONN-row state rather than call counts.</result>
<usage><subagent_tokens>103236</subagent_tokens><tool_uses>16</tool_uses><duration_ms>251913</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a69534a9f2b01fbee</task-id>
<tool-use-id>toolu_0142WjHQ4N4xLDWx9Nt2DbB9</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\a69534a9f2b01fbee.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 5: offer.ts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: 7c97da3 feat(run): the run says what a deck may press
Tests: 9/9 in offer.test.ts pass (RED confirmed first); full `apps/web/src/run` suite 198/198 pass; typecheck clean
Concerns: settled gates before TYPED/manual/finalizeUnit branching — an unsettled declareSubject/manual-with-items would get the generic message and empty presets instead of the typed preset; no test covers that combination, message text is identical either way, only presets would differ
Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-5-report.md</result>
<usage><subagent_tokens>88642</subagent_tokens><tool_uses>22</tool_uses><duration_ms>266780</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aced883dd8aa47c1c</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\aced883dd8aa47c1c.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 5 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: `manual.checklist` (pack.ts:385-388) and `finalizeUnit.confirm` (pack.ts:410-415) are exactly the fields `offer.ts:190,197` read. Field names are correct — no silent-pressable bug.

Both named risks are clear. Now the final assessment.

### Spec Compliance
- ✅ Ruling 1 — `PRESSABLE` is a plain `Record&lt;string, "roll" | "carry-on" | "close" | "enter"&gt;`, no `infer` expression. `apps/web/src/run/offer.ts:154`.
- ✅ Ruling 2 — manual: empty `checklist` → primary `carry-on` (label = step label); non-empty → `needsPage`. `finalizeUnit`: non-empty `confirm` → `needsPage`; empty/absent falls through to `PRESSABLE` → `close`. `manual` removed from `TYPED`; `declareSubject` stays. `offer.ts:160,189-197`.
- ✅ Ruling 3 — three extra tests present and correctly targeted: manual-no-items→carry-on (`offer.test.ts:67-71`), manual-with-items→needsPage (`offer.test.ts:75-83`), finalizeUnit-with-items→needsPage (`offer.test.ts:87-95`).
- ✅ Ruling 4 — `settled` is read (not left unused), placed as an early gate mirroring `closesTheUnit`'s own gate ordering (`handsFree.ts:60`, checked before any step-kind branching), with a test (`offer.test.ts:99-103`). Concern is disclosed rather than hidden.
- ✅ Field names verified against schema: `manual.checklist` and `finalizeUnit.confirm` (`packages/rules-schema/src/pack.ts:385-388,410-415`) match exactly what `offer.ts:190,197` reads — no silent-pressable risk.
- ✅ `settled`/owed/confirm semantics agree with `handsFree.ts`'s `closesTheUnit` (`handsFree.ts:60-63`): settled gates first, `finalizeUnit.confirm` gates next, owed gates after — same relative order the implementation uses.
- ✅ Exported shapes match exactly: `Offer`, `OfferInput`, `offerOf` (`offer.ts:99-107,109-125,162`).
- ✅ Ordering for the brief's five original branches (not live / no step / typed+preset / owed&gt;0 / pressable-or-page) preserved verbatim (`offer.ts:167-203`).
- ✅ Pure function, no React/engine mutation; lives beside `handsFree.ts`; comment voice matches (references `snapshot.asks`, "honor check" language lifted from `pack.ts:393`, why-not-what comments throughout).
- ✅ American English; no stray prose.
- ✅ Commit subject exact: `feat(run): the run says what a deck may press` (per diff header and report).
- ✅ Only `offer.ts` and `offer.test.ts` created; diffstat confirms nothing else touched.
- ⚠️ Cannot verify from diff: actual TDD RED/GREEN transcript and full-suite/typecheck runs — taken on the report's word per task instructions (not re-run).

### Strengths
- Placing the `settled` gate exactly where `closesTheUnit` places its own `!run.settled` check (before any step-kind branching) is a genuinely faithful precedent read, not just a plausible guess — verified against `handsFree.ts:60`.
- Comments are consistently why-not-what and reuse domain vocabulary ("honor check") rather than inventing new phrasing — `offer.ts:174-176,187-188,195-196`.
- The report's self-flagged concern about `settled` ordering vs. typed presets is accurate, precisely scoped, and honestly labeled as untested rather than smoothed over.
- Test assertions check full `primary`/`presets` object shapes where new behavior is introduced (`offer.test.ts:45,69,81,93,101`), not just truthiness.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- The flagged `settled`-before-typed ordering: an unsettled `declareSubject` returns the generic `"&lt;label&gt; on the page"` with empty `presets` instead of the typed preset (`offer.ts:177` fires before `offer.ts:179-185`). This is a reasonable call — it mirrors `closesTheUnit`'s own gate order, and `presets` existing only to seed a preset the page will ignore anyway (since the page still owns the field) makes the practical impact of missing it small. No brief or ruling test covers this combination. Worth a one-line comment at `offer.ts:174` noting the preset is intentionally dropped when unsettled, and/or a follow-up test, but not a defect to block on.
- Four branches repeat the identical `{ ...bare, primary: null, needsPage: \`${label} on the page\` }` shape (`offer.ts:177,191,197,202`). Each carries its own rationale comment so the repetition reads more like "four distinct rulings that happen to produce the same shape" than duplication, but a small local helper would trim it further if this file grows more branches later.

### Assessment
**Task quality:** Approved
**Reasoning:** All three controller rulings and the `settled`-reading requirement are implemented correctly and verified against both the schema (correct field names, ruling out the silent-pressable risk) and `handsFree.ts`'s precedent (ordering agrees, not contradicts); the one open design question is self-disclosed, low-impact, and untested by design rather than a defect.</result>
<usage><subagent_tokens>81760</subagent_tokens><tool_uses>7</tool_uses><duration_ms>114628</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a86f0a6c6e22f46ab</task-id>
<tool-use-id>toolu_01KYd9aMvkbsxRsUoofVPmtU</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\a86f0a6c6e22f46ab.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 6: publisher carries offer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All clean after the commit's prettier pass.

Status: complete
Commit: 264227b — `feat(run): the snapshot carries what a deck may press`
Tests: `RunView.offer.test.tsx` 2/2 pass (TDD RED confirmed for both, then GREEN); `apps/web/src/run` 33 files/200 tests pass; `hosted/infra` 17 files/280 tests pass (ws.ts decks-count line also RED→GREEN verified); `npm run typecheck` clean.

Concerns: `activeStepLabel` helper added beyond the brief's literal code, needed because `ActiveStep` has no `.label` field (brief's `run.activeStep?.label` doesn't exist — verified against `flow.ts`/`pack.ts`); mirrors the label logic already duplicated in `StepPanel`/`ClosingStep`/`ControlPanel`'s `RemoteStep`. Flagged a possible pre-existing gap in `dynamoLive`'s deck-watch-row marking (outside touched files, not fixed). Full report: `C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-6-report.md`</result>
<usage><subagent_tokens>253372</subagent_tokens><tool_uses>109</tool_uses><duration_ms>1081168</duration_ms></usage>
</task-notification>

---

nice catch -- let's get these changes all committed and a PR opened when we're done with the work. I also have a Stream Deck connected once we've run the headless test to directly validate with.

---

<task-notification>
<task-id>ad8a4f750941e988a</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\ad8a4f750941e988a.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 6 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed. I now have everything needed for the review.

### Spec Compliance
- ✅ Publisher gate widened to `shared || decks &gt; 0` named `publishing` — `apps/web/src/run/RunView.tsx:212`. Effect guard changed from `!shared` to `!publishing` — `RunView.tsx:217`. Dependency array has `publishing, decks` in place of `shared` — `RunView.tsx:258`.
- ✅ `useAttachedDecks(run.record?.runId ?? null)` called beside `shared` — `RunView.tsx:210`, with the brief's comment reproduced verbatim at `RunView.tsx:202-208`.
- ✅ `putSnapshot` body carries `offer: offerOf({...})` beside `paper` and `control`, with `seq: events.length` as the offer's first field — `RunView.tsx:239-250`, `snapshotOf`/`paperOf`/`control` untouched around it.
- ✅ `useAttachedDecks(runId)` added beside `useAttachedTools`, reads `data.decks` off `tools` gestures scoped to `runId`, resets to `0` on `runId` change — `apps/web/src/run/useAttachedTools.ts:291-303`.
- ✅ `tellWhoIsAttached` adds `decks: watching.filter((w) =&gt; w.deck).length` to the `tools` gesture's `data` — `hosted/infra/lib/handlers/ws.ts:331-332`.
- ✅ Page stays the one writer — no code path in the diff calls anything that moves/mutates the run; only `putSnapshot` is called, same as before.
- ✅ Only the five permitted files touched (diff stat: `RunView.offer.test.tsx` new, `RunView.tsx`, `useAttachedTools.ts`, `ws.ts`, `ws.test.ts` — matches exactly).
- ✅ Commit subject exactly `feat(run): the snapshot carries what a deck may press` (commit `264227b`).
- ✅ American English and file-voice comments: `activeStepLabel`'s new doc comment uses the file's existing "--" em-dash convention (also at `RunView.tsx:866`); the two brief-specified comments are reproduced verbatim (`RunView.tsx:202-208`, `useAttachedTools.ts:284-290`).
- ✅ Dispatch-note amendment honored: `renderRunView` was hand-built on the `App.test.tsx`/`ChatPanel.test.tsx`-style module-mock render pattern (not a nonexistent helper) — `apps/web/src/run/RunView.offer.test.tsx:51-108`; `useAttachedDecks` is driven via a real `syncBus.emit` of a `tools` gesture, not a mock — `RunView.offer.test.tsx:103-107`.
- ✅ Dispatch-note amendment honored: every `useRun` field the `offerOf` block reads was checked against `useRun.ts`'s actual return shape. Verified independently: `started` (`useRun.ts:212`), `readOnly` (`:948`), `pending` (`:128`), `activeStep`/`ActiveStep = {phase,step,index}` with no `.label` (`packages/engine/src/flow.ts:16-20`, `useRun.ts:422`), `moves: Array&lt;{id, move: MoveDef}&gt;` with `MoveDef.label: string` required (`packages/engine/src/execute.ts:789-800`, `packages/rules-schema/src/pack.ts:687-689`), `canUndo` (`useRun.ts:857`), `blockingObligations` (`:1103`). The brief's non-existent `run.activeStep?.label` was correctly replaced by the new `activeStepLabel(pack, run.activeStep)` helper rather than left broken.
- ✅ Dispatch note's extra server test present: `hosted/infra/test/ws.test.ts:359` extends the existing "marks a deck's connection... tells the table" test to assert `data: { decks: 1 }` on the `tools` gesture posted after a deck's `watch`.
- ⚠️ Cannot verify from diff: whether `dynamoLive`'s real two-row watcher model actually threads `deck: true` onto the per-session watch row `watchers()` reads (only the in-memory test double is exercised here). The diff's new `ws.ts:331` line (`watching.filter((w) =&gt; w.deck).length`) does depend on that row carrying `deck` — per the controller's ruling this is a Task 4 defect fixed in Task 7, out of scope for Task 6's touched files, and the implementer flagged it explicitly in the report's "Concerns" section rather than silently leaving it. Not a Task 6 defect.

### Strengths
- `activeStepLabel` (`RunView.tsx:92-107`) is a byte-for-byte mirror of `StepPanel`'s per-`step.kind` label logic — verified all five cases against `StepPanel`/`ClosingStep` (`RunView.tsx:1319`, `:1355`, `:1393`, `:1416`, `:1516`), including the `manual`/`closesUnit` boundary against the engine's own `closesUnit()` (`packages/engine/src/flow.ts:123-125`). No third spelling introduced.
- `live`/`settled`/`owed` in the `offerOf` input are computed from exactly the concepts `OfferInput`'s own doc comments name (`apps/web/src/run/offer.ts:28-38`): `live` = started &amp;&amp; editable &amp;&amp; not ended; `settled` = `pending === null`; `owed` = `blockingObligations.length`.
- The new test drives `useAttachedDecks` through a genuine `syncBus.emit` gesture rather than mocking the hook, and asserts the actual `putSnapshot` call body rather than a stand-in — real coverage of both named cases.
- Implementer's self-report is candid about a scope boundary (the `dynamoLive` watch-row dependency) instead of silently glossing over it.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `activeStepLabel` (`RunView.tsx:92-107`) is now a third independent copy of the step-heading switch, alongside `StepPanel`'s (`:1314-1438`) and `ControlPanel.tsx`'s `RemoteStep` (`ControlPanel.tsx:314-406`) — and the diff didn't touch either existing copy, so a future edit to one heading rule (adding a case, changing a fallback) can silently drift from the other two without a compiler complaint. Since `StepPanel` lives in the same touched file (`RunView.tsx`), extracting one shared helper both `StepPanel` and `activeStepLabel` call was possible within the file-scope constraint; not doing so is a reasonable, low-risk call for a task scoped narrowly to the offer, but it is duplication worth collapsing later.
- Test 1 (`RunView.offer.test.tsx:111-116`) asserts `offer.seq` only via `expect.any(Number)`, not the concrete value (`events.length === 1` for this fixture). The production code path is independently verified correct by reading `offer.ts`/`RunView.tsx`, so this doesn't undermine the verdict, but the test itself doesn't pin the value it's nominally about.
- Noted in the report: the jsdom `HTMLCanvasElement.getContext` console warning is pre-existing `preloadDice3d()` behavior unrelated to this change, caught internally, and doesn't affect any assertion — correctly judged as noise, not a defect.

### Assessment
**Task quality:** Approved
**Reasoning:** All brief and global-constraint requirements are met with file:line evidence, all three named risks (useRun field shapes, `activeStepLabel` fidelity to the page's real heading, and the dependency array) check out against the actual source, and the tests exercise the real `useAttachedDecks` path rather than a mock; only minor polish items (a third duplicated label switch, one under-specific assertion) remain.</result>
<usage><subagent_tokens>103046</subagent_tokens><tool_uses>26</tool_uses><duration_ms>201743</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a1b402695dc88db97</task-id>
<tool-use-id>toolu_011ZhXoJphtNTvmKETzjgZXG</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\a1b402695dc88db97.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 7: drive forwarded" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: ed9b17b feat(live): a deck's press reaches the device holding the run
Tests: RED confirmed first (4 failures, including the pre-existing deck-attach test surfaced by the faithful double), then GREEN — `ws` suite 41/41, full `hosted/infra` suite 283/283, `npm run typecheck` clean.
Concerns: none; `s2` in the fixture has no `getSession` entry (resolves to no session) so `s3` (owned by `user_2`) was added for the "somebody else's run" test, as the brief anticipated.
Report: C:\Users\xbox\ScrtGit\sbm-ui\.superpowers\sdd\2026-09-14-the-deck-drives-plan-a\task-7-report.md</result>
<usage><subagent_tokens>130525</subagent_tokens><tool_uses>28</tool_uses><duration_ms>348873</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a926fe853d5006d55</task-id>
<tool-use-id>toolu_01TjtD7f1J64m85YjSjGdjSX</tool-use-id>
<output-file>C:\Users\xbox\AppData\Local\Temp\claude\C--Users-xbox-ScrtGit-sbm-ui\9994f719-da1d-46a6-aff1-a95b696e89ae\tasks\a926fe853d5006d55.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 7 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All verification complete. Findings below.

### Spec Compliance
- ✅ `drive` branch placed after the control `event` branch (`ws.ts:371`) and before the `public:`/`stream:` early return (`ws.ts:534`-ish) — confirmed at `ws.ts:440-492`.
- ✅ `drive` requires `conn.deck` — `ws.ts:451`.
- ✅ Wire shapes exact: deck→server consumed fields `run/seq/ref/press/move?/answer?` (`ws.ts:474-481`); server→writer `{t:"drive",from,run,seq,ref,press,move?,answer?}` (`ws.ts:474-481`); page→server `drove` with `to` (`ws.ts:502`); server→deck `drove` with `to` stripped (`ws.ts:509-513`).
- ✅ Refusal strings verbatim: "That run is not yours to press." (`ws.ts:462`), "That run has ended." (`ws.ts:463`), "Nothing is holding that run." (`ws.ts:466`); 400 body "a press has a run and a ref" (`ws.ts:461`).
- ✅ `writes()` reused unmodified as sole writer predicate (`ws.ts:85-87`, `ws.ts:465`), not re-spelled.
- ✅ Server stores nothing about a press — no `deps.store` writes in either branch, only a `getSession` read (`ws.ts:458`).
- ✅ Ruling's extension 1: `watch` branch now passes marks to `deps.live.watch` — `conn.deck ? { deck: true, run: m["id"] } : undefined` (`ws.ts:648`, confirmed against diff line 135).
- ✅ Verified against production: `dynamoLive.watch()` writes `marks(attached)` onto the `SESSION#…/CONN#…` row that `watchers()` queries (`hosted/infra/lib/handlers/live.ts:132-138,149-160`) — the fix is correct for production, not just for the test double.
- ✅ Ruling's extension 2: `memoryLive()` rebuilt as a faithful two-row double — `watchMarks` keyed by `${sessionId}|${connectionId}`, written only by `watch()`, read only by `watchers()`; `connection()`/`decksOf()` keep reading `marks` from `connect()`; `disconnect` clears both (`hosted/infra/test/ws.test.ts:21-70`).
- ✅ New test proving a deck watching `s1` is not among the writers — `hosted/infra/test/ws.test.ts:1033-1046` (deck1 also sends `watch`, asserts exactly one `drive` posted, to `page` only).
- ✅ `drove` branch has explicit `public:`/`stream:` guard at its top (`ws.ts:503-506`), with its own explanatory comment in the file's voice.
- ✅ Tests use the file's real `deps()`/`ev()` helpers (pre-existing at `ws.test.ts:89,194`), not the brief's inline event-literal style.
- ✅ `s3` fixture added, owned by `user_2` only (`ws.test.ts:144`), used correctly since `deck1` authenticates as `user_1` via the `"good"` token (`ws.test.ts:187`).
- ✅ Existing test `"marks a deck's connection with the run it watches, and tells the table"` (`ws.test.ts:389-409`, untouched by the diff) went red under the faithful double and green again purely from the `ws.ts` production fix — confirmed its `decks:1` assertion traces to `tellWhoIsAttached`'s `watching.filter((w) =&gt; w.deck).length` (`ws.ts:220`), which reads `watchers()`. No test assertion was weakened; the fix lives in production code as required.
- ✅ Commit subject exact: "feat(live): a deck's press reaches the device holding the run".
- ✅ Only `hosted/infra/lib/handlers/ws.ts` and `hosted/infra/test/ws.test.ts` touched (per diff stat).
- ✅ Named risk 2 (drove route): checks both `target.deck` and `target.sub === conn.sub` before posting (`ws.ts:508`); an unresolved/expired `to` (`target` null) is a silent 200 (`ws.ts:508`), not a throw.
- ✅ Named risk 3 (drive forwarding loop): a `"gone"` result disconnects the writer, a post error is caught and logged per-iteration, and neither aborts the loop or the route (`ws.ts:483-489`) — matches the file's standing idiom used elsewhere (e.g. the `ask`-forwarding loop just above it, `ws.ts:424-428`).
- ⚠️ Cannot verify from diff: full-suite behavior beyond the reported 283/283 (not re-run per task instructions; spot-checked ~10 pre-existing `live.watch(...)` call sites across the file and found each either passes explicit marks or relied on no marks either way, consistent with the report's claim that no other test needed a setup change).

### Strengths
- The `drive`/`drove` code is copied from the brief's own snippet verbatim except for the ruling's mandated guard line — no scope creep, no re-spelling of `writes()`.
- The `memoryLive()` rewrite is a genuinely faithful two-row model (mirrors `dynamoLive`'s CONN row vs. SESSION#…/CONN# row split), not a partial patch — verified against `live.ts` directly.
- New tests assert whole posted lines with `toEqual`, including the deck-watching-its-own-run edge case explicitly required by the ruling, and correctly narrow the assertion to `drive`-typed posts only before checking count/destination.
- Self-review in the report is accurate; I did not find any unacknowledged deviation.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `ws.ts:481` — `answer` is accepted when `typeof m["answer"] === "object"`, which also admits arrays (JS `typeof [] === "object"`), not just plain objects. This is copied verbatim from the brief's own code, not an implementer deviation, so it's not chargeable to this task — flagging only as a latent brief-level looseness for whoever owns the wire contract going forward.

### Assessment
**Task quality:** Approved
**Reasoning:** Both the brief's own requirements and the controller's ruling extensions (production fix to `watch`, faithful two-row double, extra `drove` guard, `s3` fixture) are implemented correctly and verified against production code in `live.ts`; all three named risks check out; no test assertions were weakened to force green.</result>
<usage><subagent_tokens>98968</subagent_tokens><tool_uses>21</tool_uses><duration_ms>220889</duration_ms></usage>
</task-notification>